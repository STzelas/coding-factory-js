/**
 * Συναρτήσεις που επιστρέφουν true ή falase ( boolean function ) λέγονται Predicates ( σε όλες τις γλώσσες ) 
 * @param num
 * @returns true or false
 */

const isPositive = function(num) {
  return num > 0
}